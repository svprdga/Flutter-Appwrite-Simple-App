name: flutter_appwrite_simple_app
description: A simple app to showcase the features of Appwrite.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  appwrite: ^6.0.0
  file_picker: ^4.6.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true